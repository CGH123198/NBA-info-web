import SearchFormContainer from "../components/Search/SearchFormContainer";

const TeamPage = () => {
    return (
        <div>
            <SearchFormContainer placeholder="Search Teams..." page="teams" />
        </div>
    )
};

export default TeamPage;

//Teams Per Division (Atlantic, Central, SouthEast) (NorthWest, Pacific, SouthWest)
//Teams Per Conference (East, West)
//Teams Per Code (LAL, GSW, DET...)

//SearchTeam